#include "main.h"
#include <stdio.h>
/**
 * _pow_recursion - prints a string, in reverse, followed by a new line
 * @x: number raised to power of y
 * @y: nth power of x
 */
int _pow_recursion(int x, int y)
{
	if (y < 0)
		return (-1);
	else if (y == 0)
}
