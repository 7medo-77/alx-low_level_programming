#include <stdio.h>
#include "main.h"
/**
 * mul - a function that
 *
 * @a: A variable that is the first factor of multiplication
 * @b: The second variable for multiplication
 *
 * Return: Always 0 (success)
 */
int mul(int a, int b)
{
	int res;

	res = a * b;
	return (res);
}
